<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Ekran üstüne çizim yapabilmek için zorunlu -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Servisi arka planda canlı tutmak istiyorsan (Android 8+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Pil optimizasyonundan muaf tutulma isteği (servisin öldürülmemesi için) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Cihaz yeniden başladığında overlay'i otomatik başlatacaksan -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application ...>

        <service
            android:name=".FrameOverlayService"
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

    </application>
</manifest>
